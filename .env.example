# Example .env file - Auto-generated by setup.sh or create-env.sh
# You can also manually copy this file to .env and fill in your values

# ============================================================================
# Vision Agent - Gemini API Key (REQUIRED)
# ============================================================================
# Get your free API key from: https://aistudio.google.com/apikey
# The Vision Agent uses Gemini API directly (no GCP project needed for this part)
GEMINI_API_KEY=your-gemini-api-key

# ============================================================================
# Supplier Agent - GCP Project (REQUIRED)
# ============================================================================
# The Supplier Agent requires a GCP project for AlloyDB and Vertex AI embeddings
GOOGLE_CLOUD_PROJECT=your-project-id

# ============================================================================
# AlloyDB Database (REQUIRED)
# ============================================================================
# Password from infrastructure setup (./setup.sh Step 4)
# If password contains special chars like $ or ', wrap in single quotes when exporting
DB_PASS=your-alloydb-password

# ============================================================================
# AlloyDB Connection Details (Auto-detected by setup.sh)
# ============================================================================
# These are auto-populated when you run setup.sh
# Only edit manually if auto-detection fails
ALLOYDB_CLUSTER=my-codelab
ALLOYDB_INSTANCE=my-codelab
ALLOYDB_REGION=us-central1
ALLOYDB_PROJECT=your-project-id

# Database connection via Auth Proxy (localhost)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_NAME=postgres
